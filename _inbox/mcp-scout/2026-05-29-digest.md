---
profile: mcp-scout
skill: topic-sweep
generated_at: 2026-05-29T15:15:00-04:00
notebook_id: a4ca2b00
sources_ingested: 2
findings: 5
verdicts: { INTEGRATE: 1, AUDIT: 2, DOCUMENT: 2 }
proposal_status: proposed
private_payload_redacted: true
run_mode: v1-manual-bootstrap
---

# MCP & Agentic Patterns Scout — Digest 2026-05-29

**Since last digest:** first digest (bootstrap). Baseline = the fleet's own contracts (`_meta/decisions/` event-contract + agent-shape ADRs, per-project `.mcp.json`, the A2A-card shape).

**Signal:** active — the MCP spec and the A2A standard both moved into production-grade governance this window, and two of the five findings map directly onto patterns the fleet already half-implements (skills-as-contract, plan-execute-verify). The deltas worth Alex's attention are the stateless-MCP migration and signed A2A agent cards.

> Run mode: v1 manual bootstrap (sources hand-ingested, findings drawn from a real research-stack-style sweep of the MCP spec/registry, the A2A project site, and arXiv). The weekly cron (`mcp-scout-weekly-sweep`, Sat 6:10am) runs the full `/research-stack --deep --youtube` sweep going forward.

## Findings

**F1 — MCP release candidate locked: stateless protocol core + Extensions framework.** The next MCP spec RC removes `Mcp-Session-Id` (SEP-2575), makes the protocol stateless at the wire layer, and adds a formal Extensions framework, MCP Apps, Tasks (long-running work), auth hardening, and W3C trace propagation (SEP-414). Source: `blog.modelcontextprotocol.io/posts/2026-07-28-release-candidate/` + `modelcontextprotocol.io/seps/2575-stateless-mcp` (ingested). **Verdict: AUDIT → cross-project architecture** — stateless = remote MCP servers can sit behind plain round-robin LBs shared by every project profile, collapsing the cost of one MCP server farm. First question: which of the fleet's stdio servers are session-stateful in ways that block the migration? Trace-context standardization also fixes cross-project Langfuse correlation.

**F2 — Agent Skills shipped as an open standard with cross-vendor adoption.** Anthropic released Agent Skills as an independent spec (agentskills.io) adopted by VS Code, Cursor, Goose, Codex, Gemini CLI, Copilot and others — a SKILL.md folder works identically across 20+ platforms. Source: `agentskills.io/specification` + `anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills`. **Verdict: DOCUMENT → cross-project skills** — the fleet already uses SKILL.md folders (`~/.claude/skills/`, `~/.agents/skills/staged-review/`); conformance is currently accidental. Add a one-page reference pointing the project authoring guides at the agentskills.io spec so the same skill is a deliberate contract across Claude Code + Codex. No rebuild.

**F3 — A2A protocol reached Linux Foundation governance with signed agent cards (v1.0 / v1.2).** Google's Agent2Agent moved to the LF Agentic AI Foundation, hit v1.0 (production-grade) and v1.2 (cryptographically signed Agent Cards for domain verification); 150+ orgs in production. Source: `a2a-protocol.org/latest/announcing-1.0/` (ingested) + the Google OSS blog anniversary post. **Verdict: INTEGRATE → PFOS runtime / A2A surface** — A2A is the peer of MCP (agent↔agent vs. tools/data); the `NEXT_PUBLIC_ENABLE_A2A_DISCOVERY` flag (off in prod, Gate B) sits inside this standard. Signed Agent Cards are the trust primitive that makes flag-gated cross-project discovery viable without a central registry. Propose: align the fleet's A2A-card shape to v1.2 signing before the Gate-B unblock — Alex executes; flag-gated, reversible.

**F4 — Official MCP Registry crossed ~2,000 server entries; clients wiring it as the install path.** The registry (launched 2025) now carries versioned, capability-searchable server metadata; Claude Desktop and Cursor wire one-click registry installs. Source: `registry.modelcontextprotocol.io` + `github.com/modelcontextprotocol/registry`. **Verdict: AUDIT → cross-project architecture** — the fleet treats MCP servers as per-project `.mcp.json` drift; registry references (`install <id>@<version>`) would give reproducibility across profiles, and a publish path for the fleet's own internal servers (memory-vault helpers) to be subscribed rather than copied. First pass: which active `.mcp.json` servers have registry entries vs. live as one-off forks, and which of ours are publishable.

**F5 — "Verified Multi-Agent Orchestration" (ICLR 2026 workshop) formalizes the plan-execute-verify loop.** A verification-gated orchestration framework where specialized agents are coordinated through a verifier that re-enters the loop until a measurable check passes. Source: `arxiv.org/abs/2603.11445`. **Verdict: DOCUMENT → cross-project** — this is the research mirror of the fleet's Karpathy-ladder + Codex-as-reviewer doctrine (orchestrator/verifier split, falsifiable test gate). Add the arXiv ID to `~/.claude/references/compound-engineering-policy.md` as external validation of the test-gate rule. No behavior change.

## Watch next

- MCP RC → final spec divergence before 2026-07-28 (what changes in the Extensions / Tasks / Apps shape).
- Whether agentskills.io publishes a v1.1 with manifest-signing — directly hits fleet-wide tool contracts.
- A2A v1.2 reference implementations worth borrowing for the PFOS Gate-B discovery wiring.
