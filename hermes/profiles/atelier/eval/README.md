# Atelier eval suite

Eval cases are deferred until skills land. Per Phase 7.4 of `~/.claude/plans/snug-crafting-fox.md`,
the four skill SKILL.md files are authored next; eval cases follow once each skill has a
stable input/output contract.

The suite manifest is `suite.json` (referenced by `a2a-card.json` § `eval_suite_uri`).
