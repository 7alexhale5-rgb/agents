# Handoff: Phase 4.7 PrettyFly Runtime — pre-work shipped + review returned FIX THEN SHIP

**Date**: 2026-05-06
**Session**: bare-metal pivot, ADR-006, pre-work shipped, /review-stack --deep --audit, /handoff
**Project**: agents (`~/Projects/agents/`)

## What Was Done

- Synthesized Jack Roberts' "build-it-yourself" thesis from 10 video sources + 11 cross-references → `~/Projects/research-vault/research/2026-05-06-jack-roberts-thesis-bare-metal-hermes-replacement.md`
- Operator committed bare-metal-first via 6 locked yeses → ADR-006 at `_meta/decisions/2026-05-06-prettyfly-runtime-bare-metal.md`; ADR-001 amended in place
- Wrote detailed Phase 4.7 PLAN.md (~5,400 words, 5 sub-phases, 5 cutover gates, 3 pre-greenlight gates) at `.planning/phase-4-7-prettyfly-runtime/PLAN.md`
- Shipped pre-work artifacts: SPEC.md + MEMORY_LIFECYCLE.md + SKILL_SELF_GEN_BOUNDS.md + ADAPTER_PLUGIN_INTERFACE.md (PF Runtime design docs); LAIK MCP boundary SPEC; pyproject.toml + Python stubs (mypy strict clean); 3 ops scripts (hermes-commit-watcher with launchd loaded, hermes-feature-audit, profile-dir-contract test); audit baseline at `ops/audit/STATUS.md`
- First Hermes commit-watcher run captured 263 commits behind upstream — already drifted from 261 the day before
- Ran `/review-stack --deep --audit` with 6 perspectives (skeptic, architecture, security, code-quality, test-coverage, plan-conformance) + parallel Codex `$staged-review`; verdict **FIX THEN SHIP** with 1 critical + 8 high + 7 medium

## Current State

- 29 files staged on `agents/` main branch; **NOT committed** yet (review verdict requires fixes first)
- Hermes v0.12.0 pinned; do not run `hermes update`
- Phase 1 (personal profile shadow vs gravity-claw) running unchanged on Hermes
- G1 Hermes baseline measurement: 7-night capture starts tonight via existing `email-triage-eval-nightly.sh` launchd job
- launchd jobs loaded: `com.prettyfly.hermes-commit-watcher` (daily 02:30 ET)
- Codex review output saved at `/tmp/codex-review-2026-05-06.log` (362KB; verdict NEEDS WORK with 3 HIGH + 1 MED)

## What's Next

1. **Apply auto-fix manifest** (~2.5 hours of focused doc edits + tiny code patches) — see "Critical" + "Medium" items below
2. Re-run audit chain: `python3 tests/spec_self_consistency.py` + `python3 tests/profile_dir_contract.py` + `ruff check` + `mypy --strict`
3. Re-run `closeout-stack --mode=codex` for clean Codex re-review (≤2 critical findings = 4.7.0 gate cleared)
4. Update `.planning/phase-4-7-prettyfly-runtime/STATUS.md` with G2 actually-13-profiles + 4.7.0 gate cleared
5. `/commit` (the 29-file pre-work slice) then `/ship` (push corrected pre-work to main)
6. `/compound` the lessons: pre-greenlight gate ladder caught 1 critical + 8 high BEFORE any production code
7. Wait for G1 (~2026-05-13) — sub-phase 4.7.1 loop primitive starts after baseline lands

## Auto-fix manifest (priority order)

### Critical

1. Amend `_meta/decisions/2026-05-06-prettyfly-runtime-bare-metal.md` §3.7 (Kanban SQLite → Postgres) + `docs/migration-runbook.md` Phase 4.7 sub-phase 4.7.4 row
2. Change `mcp-servers/laik/SPEC.md:3` status to "DRAFT pending Phase 4.5 lead sign-off"

### High

3. Add `caller_profile_slug` + signed session binding to all LAIK tenant-scoped tools in `mcp-servers/laik/SPEC.md`
4. Replace `confirmer_user_id` (LAIK SPEC §6) with admin-UI-generated signed approval token (proposal_id, confirmer, expiry)
5. Fix `scripts/hermes-feature-audit.sh` profile iteration bug; rerun until all 13 rows populated
6. Sync 5-gate list across ADR-006 §3.7 + `docs/migration-runbook.md` + PLAN.md §11 (replace cost±10% with latency+throughput; add per-profile real-job)
7. `pf-runtime/SPEC.md` §Module layout: add note "paths are relative to project root; Python imports use `pf_runtime.` package"
8. PLAN.md §1.G1: clarify dedicated shadow workspace + Phase 1 sequencing
9. `pf-runtime/SPEC.md` Tool ABC: specify `tool_dispatch` validates args via JSONSchema before `invoke()`

### Medium

10. `pf-runtime/pf_runtime/stubs/spec_stubs.py:215`: change `def receive` → `async def receive` (verify formatter didn't already fix)
11. `pf-runtime/pyproject.toml`: add `*_consistency.py` to `python_files`
12. `pf-runtime/docs/ADAPTER_PLUGIN_INTERFACE.md`: clarify reconnect = 1 + N calls per error epoch
13. `pf-runtime/pf_runtime/stubs/spec_stubs.py:78,85,94`: add `frozen=True` on Message / InboundMessage / OutboundMessage
14. `scripts/hermes-commit-watcher.sh`: add `[ -L "$DIGEST_DIR" ] && exit 1` symlink check
15. `tests/profile_dir_contract.py`: regex-validate `profile.name` before `subprocess.run`
16. `mcp-servers/laik/SPEC.md`: add `mutation_proposals` RLS-by-tenant_id isolation note + `tests/laik_mcp_contract.py` cross-tenant assertion

## Key Decisions

- **Bare-metal-first** is the organizing principle for the agentic OS (replaces "Hermes is the runtime" framing from ADR-001)
- **6 locked operator commitments**: Phase 4.7 inserted between 4.5 and 5; Phase 1 unchanged; Hermes pinned v0.12.0; profile dirs reused; PF Runtime at `~/Projects/agents/pf-runtime/` MIT; cutover data-driven; no hybrid post-cutover
- **Three pre-greenlight gates** before any pf-runtime code is written (G0 marketplace dependency YES, G1 baseline 7 nights, G2 feature audit all 13)
- **Kanban switched to Postgres** mid-plan (SQLite would block under 13-profile concurrent load)
- **Net 3-yr ROI** if marketplace GTM depends on runtime: +$46.5K to +$426K (median +$150K) vs ~–$66K if it doesn't

## Constraints & Gotchas

- Hermes already 263 commits behind upstream after 2 days — "their roadmap, not yours" is observable in real time
- `pf-runtime/SPEC.md` cites paths like `pf-runtime/runtime/loop.py` (kebab project root) but Python package is `pf_runtime/` (snake) → `PYTHONPATH=pf-runtime` required to run tests; confusing for new contributors until SPEC §Module layout note lands
- `tests/spec_self_consistency.py` filename doesn't match `pyproject.toml` `python_files` pattern — pytest skips it silently. Either rename to `test_spec_self_consistency.py` or amend pattern
- G2 Hermes feature audit script has a profile iteration bug — only `personal` row populated even though all 13 profile dirs exist
- 29 staged files do NOT include any commit yet; review-stack returned FIX THEN SHIP, so the corrections must land before `/commit`
- Codex log at `/tmp/codex-review-2026-05-06.log` may not survive a reboot
- Phase 1 (personal profile shadow) is in flight on Hermes v0.12.0 — fix-then-ship work runs in parallel without disrupting it

## Files Modified This Session

See `~/Projects/memory-vault/sessions/2026-05-06-phase-4-7-prettyfly-runtime-charter-and-prework.md` for the full file-by-file list.

Top-level: 29 files staged (M: 4, A: 25). All on `agents/` main branch.
