---
profile: cc-scout
skill: topic-sweep
generated_at: 2026-05-29T15:10:00-04:00
notebook_id: 988d6e87
sources_ingested: 2
findings: 5
verdicts: { INTEGRATE: 1, AUDIT: 2, DOCUMENT: 1, WAIT: 1 }
proposal_status: proposed
private_payload_redacted: true
run_mode: v1-manual-bootstrap
---

# Claude Code Scout — Digest 2026-05-29

**Since last digest:** first digest (bootstrap). Baseline = the env-global config (`~/.claude/CLAUDE.md`, `~/CLAUDE.md`, `~/.claude/references/`).

**Signal:** active — the Apr–May 2026 window is genuinely heavy (a major model release + a research-preview orchestration feature + a new hook surface + an SDK breaking change). All five findings cross-checked against the canonical changelog; no fabrication risk.

> Run mode: v1 manual bootstrap (sources hand-ingested, findings drawn from a real research-stack-style sweep of the Anthropic + Claude Code changelogs). The weekly cron (`cc-scout-weekly-sweep`, Sat 6:05am) runs the full `/research-stack --deep --youtube` sweep going forward.

## Findings

**F1 — Opus 4.8 + Dynamic Workflows shipped 2026-05-28.** Opus 4.8 lands with high-effort defaults; `xhigh` is now the model default, not a deep-reasoning-only override. A "Dynamic Workflows" research preview orchestrates tens-to-hundreds of parallel subagents for codebase-scale work. Source: `anthropic.com/news/claude-opus-4-8`. **Verdict: DOCUMENT → env-global `~/.claude/`** — the `opus-4-7-operating-notes.md` reference + the `[OPUS-4-7]` CARL block + the model-pin lines in `CLAUDE.md` are now stale at the model layer (this session is already running `claude-opus-4-8`). Capture as `opus-4-8-operating-notes.md` + a model-pin refresh; the `xhigh` carve-out reframes from "when to raise" to "when to lower." No behavior change executed at rung 1.

**F2 — `MessageDisplay` hook + `disallowed-tools` skill frontmatter (v2.1.152, May 27).** Skills/slash-commands can now declare `disallowed-tools` to remove tools while active; a new `MessageDisplay` hook transforms or hides assistant text at render; `/reload-skills` re-scans without restart. Source: `code.claude.com/docs/en/changelog`. **Verdict: INTEGRATE → env-global `~/.claude/skills/`** — `disallowed-tools` is the enforced primitive `closeout-stack`/`staged-review` currently approximate by convention; `MessageDisplay` enables a post-render codename-redaction lint layer (the existing sense-8/10/15 hooks fire on tool events, none on display). The codename-redaction hook would enforce `feedback_abstract_internal_names` automatically.

**F3 — `claude agents` view + `/goal` command + pinned background sessions (v2.1.139–147, May 11–21).** `claude agents --json` lists every session for scripting; `/goal` runs to a stated completion condition; pinned background sessions survive idle and `/resume`. Source: `code.claude.com/docs/en/changelog`. **Verdict: AUDIT → env-global `~/.claude/`** — touches `cc-channels` (remote operators can address sessions by ID), the Karpathy-ladder test-gate (encodable as a `/goal` condition), and `closeout-stack` semantics (pin-or-release vs. stop-or-continue). Needs one design pass before picking the integration.

**F4 — Agent SDK v0.2.82 breaking change: `TodoWrite` → `TaskCreate/Update/Get/List` (May 15).** The single todo tool split into four discrete task tools; MCP server connect moved to background (sessions start immediately, missing-MCP errors surface mid-task not at boot). Source: `github.com/anthropics/claude-agent-sdk-python/releases`. **Verdict: AUDIT → specific projects** — any SDK harness assuming `TodoWrite` breaks silently. Grep `~/Projects/` for `TodoWrite|claude_agent_sdk` (prettyfly-os runtime is the likely caller) before the next SDK bump.

**F5 — Plugin dependency enforcement + pre-install cost projections (v2.1.143, May 13).** Plugins declare enforced dependencies (disable refuses if depended-on; enable force-enables transitive deps); `/plugin` browse shows projected per-turn token cost before install; `pluginSuggestionMarketplaces` allowlists org marketplaces. Source: `code.claude.com/docs/en/changelog`. **Verdict: WAIT → watch** — low-friction governance for `codex-plugin-cc`; pre-install cost projection ties to the $2/$5 daily Anthropic budget. Trigger: adding a second non-Anthropic plugin or publishing an internal PrettyFly marketplace.

## Watch next

- Dynamic Workflows graduating from research-preview to GA — the inflection point where `/closeout-stack`, Karpathy-ladder cadence, and the `/effort` doctrine need restructuring, not patching.
- A standalone `MessageDisplay` docs page (only the changelog one-liner exists today) — firms up the codename-redaction hook design.
- The next Agent SDK release past v0.2.82 — confirm the task-tool surface is stable before any prettyfly-os SDK bump.
