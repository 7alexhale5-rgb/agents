profile: codex-disabled-placeholder

agent:
  max_iterations: 32
  iteration_budget_tokens: 200000

model:
  default_provider: openrouter
  default_model: nvidia/nemotron-nano-9b-v2

memory:
  built_in:
    enabled: true
    fts5_enabled: true
  honcho:
    enabled: false

mcp_servers: {}
skills:
  install: []
channels: {}
approval:
  mode: explicit_for_outbound
guardrails:
  spend_cap_per_day_usd: 0
  alert_threshold_pct: 80
