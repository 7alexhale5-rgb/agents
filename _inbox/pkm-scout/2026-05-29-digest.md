---
profile: pkm-scout
skill: topic-sweep
generated_at: 2026-05-29T15:20:00-04:00
notebook_id: f181b42e
sources_ingested: 2
findings: 5
verdicts: { INSTALL: 1, INTEGRATE: 1, AUDIT: 1, DOCUMENT: 1, WAIT: 1 }
proposal_status: proposed
private_payload_redacted: true
run_mode: v1-manual-bootstrap
---

# NotebookLM & PKM Scout — Digest 2026-05-29

**Since last digest:** first digest (bootstrap). Baseline = the memory-vault wiki + `memory_hub.py --validate --strict-wiki` (the fleet's own LLM-authored second-brain).

**Signal:** active — the most actionable item this window is a real lever to retire the `notebooklm-py` cookie/Playwright fragility risk (F5), plus NotebookLM capability bumps that raise the value of automating it. The standing watch — an official **consumer** API — has NOT triggered: the Enterprise API exists but consumer remains vapor.

> Run mode: v1 manual bootstrap (sources hand-ingested, findings drawn from a real research-stack-style sweep of the NotebookLM/Google blogs, Obsidian changelog, the notebooklm-py repo, and the Karpathy LLM-wiki gist). The weekly cron (`pkm-scout-weekly-sweep`, Sat 6:15am) runs the full `/research-stack --deep --youtube` sweep going forward.

## Findings

**F1 — NotebookLM Enterprise API is real (preview); consumer API still vapor.** Google Cloud ships a documented NotebookLM Enterprise API (`notebooks.create/get/list/batchDelete/share`) gated behind a Gemini Enterprise / Education license; the @NotebookLM team still calls a consumer API "in the works" with no waitlist, timeline, or endpoint. Source: `docs.cloud.google.com/gemini/enterprise/notebooklm-enterprise/docs/api-notebooks` (ingested). **Verdict: WAIT → env-global** — the `notebooklm-py` fragility risk is NOT retired; Enterprise API only helps if Alex's access sits under a Gemini Enterprise tenant (consumer Pro/Ultra doesn't qualify). Hold the unofficial library; re-check this page every sweep. This is the standing-watch item.

**F2 — NotebookLM capability bumps (Oct 2025 → Apr 2026) raise the value of automating it.** Full 1M-token context per notebook, 6× conversation memory, persistent chat history, custom personas (Oct 29 engine upgrade); EPUB upload, cinematic video overviews, infographic export, bidirectional Gemini-app sync (April); Gemini 3.5 Flash default at I/O 2026. Source: `blog.google/innovation-and-ai/models-and-research/google-labs/notebooklm-custom-personas-engine-upgrade/`. **Verdict: INTEGRATE → research-vault** — each notebook is now meaningfully more useful per-source; "one mega-notebook per research theme" is now viable (1M context). Worth a small `notebooklm-py` workflow: per-theme notebook, append new `research-vault/research/*.md` artifacts, preserve chat history, query via persona. Caution: persistent chat history is now load-bearing — don't clobber it on reuse.

**F3 — Karpathy's "LLM Wiki" pattern (Apr 4) is the window's most-discussed PKM idea — and the vault already does it.** Three folders (`raw/` append-only sources, `wiki/` LLM-authored summaries with backlinks + index, `outputs/`), with a compile pass and a lint pass (contradictions, stale links, missing entries). Explicit "context-loading, not retrieval" — no RAG/vectors. Source: `gist.github.com/karpathy/442a6bf555914893e9891c11519de94f` (ingested). **Verdict: AUDIT → memory-vault** — `memory-vault/wiki/` + `memory_hub.py --strict-wiki` already implements this (shipped before Karpathy posted). The harvest is the **lint pass** specifically: diff his contradiction/orphan-backlink checks against `memory_hub.py` capabilities, file findings as one planning doc. Do NOT rebuild what already exists.

**F4 — Obsidian 1.13.0 (May 28) overhauled Settings + Share Sheet; URIs now require confirmation.** Desktop: settings in a separate searchable window, `obsidian://` URIs now require a confirmation prompt (security hardening), restricted-mode exit without re-enabling plugins. Mobile: redesigned iOS Share Sheet capturing full webpage/YouTube into the vault. Earlier (1.12.7) replaced the Electron CLI with a faster native binary. Source: `obsidian.md/changelog/` (ingested-candidate; keep on weekly list). **Verdict: DOCUMENT → env-global** — the URI confirmation dialog is friction for any automation firing `obsidian://` URIs from scripts (memory_hub / launchd jobs). Add a one-line note to `~/.claude/references/` so future-Claude doesn't ship a workflow that fights it; the faster CLI binary is a small win for anything shelling out to `obsidian`.

**F5 — `notebooklm-py` RFC #233 closed; cookie-import auth path shipped (May 24 release).** The maintainer rejected migrating off Playwright and instead shipped `notebooklm login --browser-cookies <browser>` (reuses your already-logged-in Chrome session) plus `NOTEBOOKLM_AUTH_JSON` for unattended CI. Source: `github.com/teng-lin/notebooklm-py/issues/233` + `github.com/teng-lin/notebooklm-py`. **Verdict: INSTALL → research-vault automation** — this is the short-term lever to cut the cookie/Playwright fragility without waiting on Google. Switch the automation to `--browser-cookies chrome` + `NOTEBOOKLM_AUTH_JSON` for cron paths; refresh via `notebooklm auth refresh --browser-cookies chrome` on a 401. Low-risk auth-path swap (TYPE-2), Alex executes. Note: the existing `com.prettyfly.notebooklm-keepalive` bridge already mitigates staleness — this complements it, doesn't replace it.

## Watch next

- **Standing watch (every sweep):** any official Google blog / @NotebookLM post announcing a consumer-API beta, waitlist, or developer preview — the single event that flips strategy from "harden `notebooklm-py`" to "migrate."
- Whether the F5 `--browser-cookies chrome` path is adopted and how it interacts with the keepalive launchd.
- Obsidian Bases search-within-bases + CSV→Markdown landing (roadmap) — relevant to vault-as-database workflows.
