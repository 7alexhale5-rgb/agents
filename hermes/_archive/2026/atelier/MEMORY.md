# MEMORY — atelier

Atelier design decisions across projects. Cross-session, append-curated (not append-only).
Mutations to this file require operator approval per the non-personal-profile policy in
`~/Projects/agents/pf-runtime/docs/MEMORY_LIFECYCLE.md`.

## Index

(Populated by the dream loop as Atelier runs. Each entry is a one-line pointer to a
decision, anchored to a project + date. Detail lives in topic files alongside this index
once the dream loop is wired.)
